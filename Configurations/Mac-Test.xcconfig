//
// This file defines additional configuration options that are appropriate only
// for a unit testing bundle on OS X. This should be set at the target level for
// each project configuration.
//

// Apply common settings specific to Mac OS X
#include "Mac-Base.xcconfig"

FRAMEWORK_SEARCH_PATHS = $(inherited) $(DEVELOPER_FRAMEWORKS_DIR)
